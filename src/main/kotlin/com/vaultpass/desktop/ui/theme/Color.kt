package com.vaultpass.desktop.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Colors (Assuming Blue as the base primary accent)
val PrimaryDark = Color(0xFF42A5F5) // Blue 400
val PrimaryLight = Color(0xFF1E88E5) // Blue 600
val OnPrimary = Color(0xFFFFFFFF)

// Surface & Background Colors
val BackgroundDark = Color(0xFF0F1115)
val BackgroundLight = Color(0xFFF8F9FA)

val SurfaceDark = Color(0xFF1A1C23)
val SurfaceLight = Color(0xFFFFFFFF)

val SurfaceVariantDark = Color(0xFF252830)
val SurfaceVariantLight = Color(0xFFE9ECEF)

// Semantic Colors
val ErrorDark = Color(0xFFFF5252)
val ErrorLight = Color(0xFFD32F2F)

val SuccessDark = Color(0xFF4CAF50)
val SuccessLight = Color(0xFF388E3C)

val WarningDark = Color(0xFFFFC107)
val WarningLight = Color(0xFFF57C00)

// Text & Typography Colors
val TextHighContrastDark = Color(0xFFFFFFFF)
val TextHighContrastLight = Color(0xFF212121)

val TextMediumContrastDark = Color(0xFFB0B3B8)
val TextMediumContrastLight = Color(0xFF5F6368)

val TextLowContrastDark = Color(0xFF5C6066)
val TextLowContrastLight = Color(0xFF9E9E9E)

val BorderDark = Color(0xFFFFFFFF).copy(alpha = 0.1f)
val BorderLight = Color(0xFF000000).copy(alpha = 0.1f)
